# Context-Budget
ZCode skill that minimizes LLM input-token cost: packs many actions into one turn, hard-caps every tool result, delegates broad exploration to subagents. A/B-tested: ~50% fewer input tokens on a full website build.
